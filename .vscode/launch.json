{
    "version": "0.2.0",
    "configurations": [
        
        {
            "name": "运行主程序",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/主程序.py",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}",
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            }
        }
    ]
}
